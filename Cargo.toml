[package]
name = "mutex-vs-rwlock-short-critical-sections"
version = "0.1.0"
edition = "2024"

[dependencies]
spin = { version = "0.10.0", features = ["mutex", "spin_mutex"] }

[features]
rwlock = []
spinlock = []
